\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{code}[2026/08/03 Code window]

\RequirePackage{tcolorbox}
\tcbuselibrary{skins, listings, breakable}

\tcbset{
    neocodetitlefont/.store in=\neocode@titlefont,
    neocodetitlefont=\small\ttfamily\bfseries,
    neocodefont/.store in=\neocode@mainfont,
    neocodefont=\small\ttfamily
}

\tcbset{
    neowinstyle/.style={
        enhanced,
        breakable,
        colback=paper,
        colframe=ink,
        boxrule=1.5pt,
        sharp corners,
        top=8mm, 
        titlerule=0pt,
        overlay unbroken and first={
            \fill[ink] (frame.north west) rectangle ([yshift=-20pt]frame.north east);
            
            \node[anchor=west, xshift=4pt, yshift=-10pt] at (frame.north west) 
                {\color{paper}\neocode@titlefont #1};
            
            \draw[paper, ultra thick] ([xshift=-12pt, yshift=-13.5pt]frame.north east) -- ([xshift=-6pt, yshift=-6.5pt]frame.north east);
            \draw[paper, ultra thick] ([xshift=-12pt, yshift=-6.5pt]frame.north east) -- ([xshift=-6pt, yshift=-13.5pt]frame.north east);
            
            \draw[paper, ultra thick] ([xshift=-24pt, yshift=-13.5pt]frame.north east) rectangle ([xshift=-18pt, yshift=-6.5pt]frame.north east);
            
            \draw[paper, ultra thick] ([xshift=-36pt, yshift=-10pt]frame.north east) -- ([xshift=-30pt, yshift=-10pt]frame.north east);
        },
        shadow={4pt}{-4pt}{0pt}{ink}, 
        before upper={\ifdefined\setLTR\setLTR\fi},
    }
}

\newtcblisting{neocode}[2][]{
    neowinstyle={#2},
    listing only,
    listing options={
        basicstyle=\ttfamily\small,
        breaklines=true,
        tabsize=2,
        showstringspaces=false,
        escapechar=@
    },
    #1
}