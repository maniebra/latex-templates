\begin{frame}[fragile]{بلوک کد}
    \begin{codebox}[language=Python]{fib.py}
def fib(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
    \end{codebox}
\end{frame}

\begin{frame}[fragile]{کد از روی فایل}
    {\small آرگومان‌ها متن نوار عنوان و مسیر فایل روی دیسک هستند، پس اسلاید
    هیچ‌وقت از کدی که نشان می‌دهد عقب نمی‌ماند.}

    \vspace{0.2cm}
    \codefile[language=Python]{code/example.py}{code/example.py}
\end{frame}

\begin{frame}[fragile]{زبانی دیگر}
    \begin{codebox}[language=SQL]{report.sql}
select date_trunc('month', created_at) as month, count(*) as shipped
from orders
where status = 'shipped'
group by 1
order by 1;
    \end{codebox}
\end{frame}
